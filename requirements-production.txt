# Exact runtime used to train and approve artifacts/20260901T191756Z-logistic.
# Keep this file synchronized with the artifact's versions.json when promoting a
# future approved model.
joblib==1.5.3
numpy==2.5.1
pandas==3.0.5
PyYAML==6.0.3
pydantic==2.13.4
scikit-learn==1.9.0
typer==0.27.0
xgboost==3.3.0

# HTTP service runtime verified with the approved model.
fastapi==0.139.2
starlette==1.3.1
uvicorn[standard]==0.51.0
