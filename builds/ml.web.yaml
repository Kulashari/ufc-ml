services:
  ml-web:
    image: ufc-ml-web:local
    build:
      context: ..
      dockerfile: builds/docker/web.Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-}
    ports:
      - "${UFC_ML_WEB_PORT:-8080}:8080"
    restart: unless-stopped
